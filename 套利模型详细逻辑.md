# ITN Token 折价（Redeem）套利模型详细逻辑

本文说明当前项目实现的套利模型，包括套利方向、价格定义、信号触发、订单顺序、每一步的延迟、RFQ 生命周期、资金冻结、失败处理和 PnL 计算。

> 本文描述的是当前回测/影子执行模型，不等同于生产环境的真实成交结果。回测使用历史 `xStocks Price (USD)` 作为可执行 Buy RFQ 价格，默认假设 redeem 最终成功；真实接入时还需要替换为正式 RFQ、真实订单回报、借券状态、链上交易状态和实际费用。

## 1. 套利方向和核心经济关系

当前只做一个方向：**Token 折价，买 Token 并 redeem 成股票**。

当 Token 的买入成本低于等效股票的可卖空收入时，执行：

```text
锁定 Token Buy RFQ
        ↓
延迟后以股票买一 bid 做保护性卖空
        ↓
股票卖空全部成交后，执行已锁定的 Token Buy RFQ
        ↓
获得 Token
        ↓
redeem Token 为等效股票
        ↓
股票到账，抵消临时空头
        ↓
结算毛利润、总成本和净利润
```

经济关系是：

```text
低价买入 Token
高价卖出等效股票
用 redeem 得到的股票归还空头
```

Token 买入成功后，Token 多头与股票空头形成配对；主要未对冲风险集中在股票空头已经成交、但 Token RFQ 还没有成功执行的短暂阶段。如果 RFQ 失败，模型会用股票 ask 紧急买回空头。

## 2. 数量和价格定义

设：

| 符号 | 含义 | 当前默认值 |
|---|---|---:|
| `q` | 每笔买入 Token 数量 | 10 Token |
| `m` | 每枚 Token 对应的股票数量 | 1.0 股/Token |
| `Q` | 等效股票数量，`Q = q × m` | 10 股 |
| `P_token` | RFQ 的 Token 单价 | 历史数据/正式 RFQ |
| `C_token` | Token 总买入成本，`C_token = P_token × q` | USD |
| `P_bid` | 股票卖空参考价格 | 股票 L1 bid |
| `P_ask` | 失败回补参考价格 | 股票 L1 ask |

当前默认配置：

```yaml
token_qty_per_trade: 10.0
token_multiplier: 1.0
```

因此每笔交易默认是买入 10 Token、卖空 10 股、redeem 得到 10 股。生产环境不得永久假设 `m = 1`，必须使用发行方提供的资产映射或每笔正式兑换数量。

### 2.1 Token 价格

回测将标准化数据中的 `xStocks Price (USD)` 作为一条历史 Token Buy RFQ 的**单枚 Token 价格**：

```text
Token 总成本 = xStocks Price (USD) × Token 数量
```

例如 Token 单价为 $99、数量为 10 时，Token 成本为 $990。每条有效响应都会形成一条独立 RFQ，不能把同一条 RFQ 当成多次可成交机会。

真实生产流程应使用正式 RFQ 返回的 quote_id、side、token_qty、total_cost、expiration、attestation/signature 和目标钱包。当前回测没有正式 quote ID 和真实链上执行回报，因此是历史可执行价格的模拟回放。

### 2.2 股票价格

信号判断使用股票 L1 bid，而不是 ask、mid 或收盘价：

```text
股票卖空名义金额 = P_bid × Q
```

股票订单真正到达市场时，再读取此时之前最后收到的股票快照：

```text
实际卖空成交价
= 到达时最新 bid × (1 - short_slippage_bps / 10,000)
```

默认 short_slippage_bps 为 0，所以默认实际卖空价就是到达时 bid。信号时的 bid 只用于发现机会、计算预计保证金和生成保护限价，不能直接假设延迟后仍按信号 bid 成交。

如果股票空头已成交但 Token RFQ 失败，回补使用到回补事件时最新有效的 ask：

```text
回补价格 = ask × (1 + cover_slippage_bps / 10,000)
```

默认 cover_slippage_bps 为 2，即 ask 上额外加 2bps。

## 3. 信号触发条件

### 3.1 股票行情有效性

股票行情只有同时满足以下条件，才允许触发信号：

```text
bid 为有限数值
ask 为有限数值
Quote Age ≤ max_stock_quote_age_s
Quote Stale = false
```

当前 max_stock_quote_age_s 为 0.5 秒，因此行情超过 500ms 或被标记为 stale 时，不开新单，也不会被用于正常股票成交判断。

### 3.2 毛 edge

对当前最便宜、仍有效的 RFQ：

```text
stock_notional       = P_bid × Q
gross_pnl_at_signal  = stock_notional - C_token
gross_edge_bps
= (P_bid × Q - C_token) / (P_bid × Q) × 10,000
```

如果 m 固定，也可以写成：

```text
gross_edge_bps
= (P_bid - P_token / m) / P_bid × 10,000
```

### 3.3 最低 edge 要求

当前配置：

```yaml
total_cost_deduction_bps: 15.0
minimum_net_profit_bps: 0.0
```

所以：

```text
required_gross_edge_bps
= total_cost_deduction_bps + minimum_net_profit_bps
= 15bps
```

触发条件是严格大于：

```text
gross_edge_bps > required_gross_edge_bps
```

正好只有 15bps 的毛 edge，在当前模型中不会交易。

### 3.4 保护性卖空最低价格

模型计算保护性卖空限价：

```text
P_min
= (C_token / Q)
  / (1 - required_gross_edge_bps / 10,000)
```

股票实际到达时，只有：

```text
实际卖空成交价 > P_min
```

才认为保护性限价通过。当前代码采用严格大于，正好等于 P_min 也会拒绝。模型对应一次性 FOK/严格 IOC 尝试，不会挂单等待未来价格变好。

## 4. RFQ 生命周期和 QuoteBook

当前配置：

```yaml
ttl_seconds: 30.0
available_from: response_receive_time
expiry_gate: confirmation
expiry_safety_buffer_ms: 500
one_time_use: true
```

因此：

```text
quote_received_at = Resp Recv (UTC)
expires_at        = quote_received_at + 30秒
valid_until       = expires_at - 500ms
```

RFQ 在 Resp Recv 之前不可使用。RFQ 状态为：

```text
active → reserved → used
   ↓         ↓
expired   expired

reserved → active  （股票腿失败且报价仍未过期）
```

状态含义：

- active：可被策略选中。
- reserved：已分配给一笔交易，其他交易不能再使用。
- used：已执行，永久消耗。
- expired：过期或被判定为不能安全执行。

每个决策时刻，策略从所有尚未过期的 active RFQ 中选择 Token 单价最低的一条。固定每笔数量时，更贵报价的毛 edge 更低；如果最低价报价因 edge 或资金不足不通过，更贵报价也不会通过。如果最低价报价因预计无法在 TTL 内执行而被丢弃，策略会继续检查下一条。

## 5. 一笔套利的完整执行步骤

以下以信号时刻 t0 为起点，使用当前默认延迟参数。

### 步骤 0：持续接收行情和 RFQ

数据预处理只保留：

```text
HTTP Status = 200
Symbol = AAPL
RFQ Token 价格、股票 bid、ask 均非空
```

然后按 Resp Recv (UTC) 排序，逐条送入回测引擎。每收到一条新响应，系统依次：

1. 处理已经到期的内部事件；
2. 使旧 RFQ 过期；
3. 更新最新股票 L1 快照；
4. 把新 Token RFQ 加入 QuoteBook；
5. 评估是否触发一笔或多笔交易。

### 步骤 1：计算机会并锁定 RFQ，t0

策略使用当前最新股票 bid 与 QuoteBook 最低 Token 价格计算 Token 成本、股票名义金额、毛 edge 和保护性卖空限价，并检查：

```text
RFQ 是否仍 active
预计 RFQ 执行完成是否早于 expires_at - safety_buffer
是否达到 max_open_trades
可用现金是否足够
```

通过后，RFQ 从 active 改为 reserved，并立即冻结：

```text
预计 Token 成本 = P_token × q
预计空头保证金 = signal_bid × Q × short_margin_ratio
```

默认 short_margin_ratio 为 1.5。

### 步骤 2：策略计算并发出股票订单，t0 + 10ms

```yaml
strategy_compute: 10ms
```

创建 Alpaca 模拟卖空订单：

```text
side      = sell_short
symbol    = AAPL
quantity  = Q
order     = top-of-book FOK / 严格 IOC
limit     = P_min
```

### 步骤 3：股票订单到达市场，t0 + 60ms

默认股票到达延迟：

```text
strategy_compute = 10ms
alpaca_submit    = 20ms
exchange_route   = 30ms
合计             = 60ms
```

订单到达时，模型只使用此前最后收到的股票快照，取 arrival_bid、arrival_ask 和 arrival_bid_size。实际卖空价为：

```text
fill_price = arrival_bid × (1 - short_slippage_bps / 10,000)
```

全额成交必须同时满足：

```text
fill_price > P_min
available_bid_size ≥ Q
空头保证金仍然足够
```

同一股票行情快照的 BIDSIZE 会被已成交的并发订单扣减，不能被多笔订单重复使用。

如果任意条件不满足：

```text
股票 FOK/IOC 拒绝
不执行 Token RFQ
报价未过期时释放 RFQ reservation
交易记为 stock_rejected，PnL = 0
```

如果股票行情不存在、过期或 RFQ 已在等待股票时过期，也会拒绝股票腿。

### 步骤 4：收到股票成交回报，t0 + 110ms

```yaml
stock_fill_report: 50ms
```

股票成交后再等待 50ms，策略才认为成交状态已确定：

```text
股票空头数量 = Q
股票卖空收入 = fill_price × Q
空头未对冲数量 = Q
```

只有全额成交才进入下一步；当前 top_of_book_fok 模型不把部分成交作为成功成交。

### 步骤 5：执行已锁定的 Token Buy RFQ，确认时约 t0 + 1.73s

收到股票成交回报后，创建 rfq_buy_token 订单，使用此前锁定的 Token 价格，不重新使用未来价格。

默认 RFQ 执行延迟：

```text
rfq_submit        = 20ms
fireblocks_policy = 0ms
fireblocks_sign   = 500ms
tx_broadcast      = 100ms
chain_confirm     = 1,000ms
合计              = 1,620ms
```

因为 expiry_gate 为 confirmation，当前模型把链上确认视为 RFQ 执行完成点：

```text
RFQ 执行检查时间 = 股票成交回报时间 + 1,620ms
```

只有该时间满足：

```text
执行检查时间 ≤ expires_at - 500ms
```

且 QuoteBook 中 RFQ 仍为 reserved，才执行成功并将 RFQ 标记为 used。

如果超过有效时间：

```text
RFQ 标记为 rfq_expired
不买入 Token
不进入 redeem
创建紧急回补订单
```

### 步骤 6：Token 到账并锁定收益，约 t0 + 1.73s

RFQ 成功确认后：

```text
钱包 Token 数量增加 q
股票空头与 Token 多头配对
未对冲空头数量降为 0
现金扣除 Token 总买入成本
```

单笔 PnL：

```text
gross_pnl = stock_short_proceeds_usd - token_buy_cost_usd

total_cost = stock_short_proceeds_usd
             × total_cost_deduction_bps / 10,000

net_pnl = gross_pnl - total_cost
```

此时净 PnL 记录为 locked_net_pnl，但正式已实现收益要等 redeem 完成。

### 步骤 7：提交 redeem，Token 到账后约 +100ms

```yaml
redeem_submit: 100ms
```

Token 到账后 100ms，创建 itn_redeem 订单，请求将 q 枚 Token 兑换为 Q 股等效股票：

```text
wallet_token_qty   -= q
redeem_pending_qty += Q
```

### 步骤 8：redeem 完成并抵消空头，约 t0 + 4.83s

默认 redeem 后续延迟：

```text
redeem_submit        = 100ms
redeem_chain_confirm = 1,000ms
issuer_process       = 1,000ms
alpaca_journal       = 1,000ms
合计                 = 3,100ms
```

成功路径总生命周期约为：

```text
股票到达市场       60ms
股票成交回报       50ms
RFQ 到链上确认   1,620ms
redeem 全流程    3,100ms
--------------------------------
合计约          4,830ms
```

redeem 完成时，redeem_pending_qty 减少 Q，股票仓位增加 Q，原有空头归零，保证金释放，卖空所得结算入现金，正式已实现净 PnL 更新。

当前模型假设 redeem 最终成功，不模拟 redeem 失败、长时间 pending、发行方拒绝或 journal 失败。因此 redeem 延迟影响库存和资金占用时间，但不改变成功路径的单笔 PnL。

## 6. RFQ 失败后的紧急回补

最重要的失败场景是股票已经卖空，但 RFQ 到期或执行失败。

触发条件：

```text
股票卖空已全额成交
但 RFQ 在确认时超过 valid_until
或 RFQ 已不再是 reserved
```

回补延迟：

```yaml
emergency_cover_submit: 50ms
emergency_cover_route: 50ms
```

从发现 RFQ 失败到回补订单到达市场默认约 100ms。回补价格和 PnL：

```text
cover_price = latest_ask × (1 + 2bps / 10,000)
cover_cost  = cover_price × Q
gross_pnl   = stock_short_proceeds - cover_cost
total_cost  = stock_short_proceeds × 15bps / 10,000
net_pnl     = gross_pnl - total_cost
```

如果回补时没有有效股票行情，交易记为 unresolved_end_of_data，不作为已实现成功 PnL 统计，必须单独查看。

## 7. 资金、保证金和并发控制

当前初始本金为 20,000 USD：

```yaml
initial_capital_usd: 20000.0
```

可用资金：

```text
available_cash
= cash_balance - reserved_token_cost - reserved_short_margin
```

卖空所得在空头未关闭前不计入可重复使用现金，避免把临时卖空收入错误当作可再次开仓的本金。

信号触发、股票下单前先冻结：

```text
Token 成本冻结 = P_token × q
空头保证金冻结 = signal_bid × Q × 1.5
```

只有 available_cash 足够 Token 成本加预计空头保证金，才允许创建交易。股票实际成交后按实际卖空收入校准：

```text
actual_margin = actual_short_proceeds × 1.5
```

max_open_trades 默认为 null，表示不设置人为笔数上限，但可用资金、RFQ 一次性使用和同一快照 BIDSIZE 仍然限制并发。配置正整数后，达到上限即停止触发新交易。

## 8. PnL 计算示例

假设：

```text
Token 数量 q       = 10
兑换比例 m         = 1
股票数量 Q         = 10
Token 单价         = $99.00
股票卖空成交价     = $100.00
统一成本           = 15bps
```

计算：

```text
Token 买入成本 = 99.00 × 10 = $990.00
股票卖空收入   = 100.00 × 10 = $1,000.00
毛 PnL         = 1,000.00 - 990.00 = $10.00
总成本         = 1,000.00 × 15 / 10,000 = $1.50
净 PnL         = 10.00 - 1.50 = $8.50
```

毛 edge 为 100bps，高于 15bps 要求。保护性最低卖空价格：

```text
P_min = (990 / 10) / (1 - 15 / 10,000)
      ≈ $99.1487
```

实际卖空成交价为 $100，高于保护价，因此股票腿通过。

## 9. 事件和最终状态

回测引擎按 received_ts_ns 从早到晚处理数据，并对延迟事件使用优先队列。主要状态顺序：

```text
quote_reserved
    ↓
stock_filled / stock_rejected
    ↓
rfq_buy_token
    ↓
token_received 或 rfq_expired
    ↓                         ↓
redeem_submitted          emergency_buy_to_cover
    ↓                         ↓
redeem_completed          emergency_covered
```

| 最终状态 | 含义 |
|---|---|
| stock_rejected | 股票订单未满足保护价、BIDSIZE、行情或保证金条件；没有买 Token。 |
| rfq_expired | 股票已卖空，但 RFQ 没有在安全截止时间前确认；随后尝试回补。 |
| completed | RFQ 成功、Token redeem 完成、股票抵消空头。 |
| unresolved_end_of_data | 数据结束前交易仍未闭合，无法判断最终结果。 |

## 10. 当前默认延迟参数

| 阶段 | 配置 | 默认延迟 | 作用 |
|---|---|---:|---|
| 策略计算 | strategy_compute | 10ms | 收到信号到发起股票订单 |
| Alpaca 提交 | alpaca_submit | 20ms | 股票订单提交至 Alpaca |
| 交易所路由 | exchange_route | 30ms | Alpaca 到市场 |
| 股票成交回报 | stock_fill_report | 50ms | 市场成交到策略收到回报 |
| RFQ 提交 | rfq_submit | 20ms | 发起 Token RFQ 执行 |
| Fireblocks 策略 | fireblocks_policy | 0ms | 自动策略检查/审批 |
| Fireblocks 签名 | fireblocks_sign | 500ms | 签名交易 |
| 链上广播 | tx_broadcast | 100ms | 交易广播到链/RPC |
| Token 链上确认 | chain_confirm | 1,000ms | Token 买入确认并可用 |
| Redeem 提交 | redeem_submit | 100ms | 发起 redeem |
| Redeem 链上确认 | redeem_chain_confirm | 1,000ms | redeem 链上确认 |
| 发行方处理 | issuer_process | 1,000ms | 发行方处理并通知 |
| Alpaca journal | alpaca_journal | 1,000ms | 股票到账并抵消空头 |
| 紧急回补提交 | emergency_cover_submit | 50ms | RFQ 失败后发起回补 |
| 紧急回补路由 | emergency_cover_route | 50ms | 回补订单到市场 |

关键合计：

```text
股票订单到市场：10 + 20 + 30 = 60ms
股票成交回报：  额外 50ms
RFQ 到确认：    20 + 0 + 500 + 100 + 1,000 = 1,620ms
Redeem 到完成：  100 + 1,000 + 1,000 + 1,000 = 3,100ms
成功路径总生命周期约：60 + 50 + 1,620 + 3,100 = 4,830ms
```

这些是回测配置值，不是 Alpaca、发行方、Fireblocks 或区块链的服务级承诺。生产运行前应使用真实测试和执行日志校准 p50、p95、p99。

## 11. 数据、输出和回测边界

当前数据流程：

1. 读取 CSV；
2. 保留 HTTP 200、目标标的 AAPL、价格和 L1 不为空的行；
3. 将 Req Sent (UTC) 和 Resp Recv (UTC) 转为 UTC 纳秒时间戳；
4. 按 Resp Recv (UTC) 排序；
5. 每条有效响应加入 QuoteBook；
6. 只允许使用当前时点以前已经收到的股票和 RFQ 信息。

当前明确不模拟：

```text
真实 formal quote ID / attestation
实际股票成交概率和排队位置
借券可用性、借券费、强平
真实 Gas、链上拥堵和交易失败
redeem 失败或长期 pending
稳定币与 USD 的实际兑换成本
RFQ 价格在真实 API 中的逐笔重报价
```

结果适合回答历史机会数量、延迟增加后剩余 edge、15bps 总成本后的模拟 PnL，以及资金、BIDSIZE 和 RFQ 一次性使用对并发的限制；不适合直接宣称生产可成交笔数、真实成交成功率、真实可实现收益或真实风险敞口。

关键输出：

| 文件 | 用途 |
|---|---|
| opportunity_series.parquet | 每个时刻的最佳 RFQ、bid、edge、保护价和未触发原因。 |
| trade_details.parquet | 每笔交易的 RFQ、股票成交、Token 到账、redeem 时间、资金和 PnL。 |
| order_details.parquet | 股票卖空、Token 买入、redeem、紧急回补订单的提交、到达、成交或拒绝。 |
| portfolio_state_series.parquet | 现金、保证金、Token、redeem pending、空头库存和累计收益。 |
| summary.json | 总笔数、完成笔数、失败状态、毛 PnL、总成本和净 PnL。 |
| resolved_config.yaml | 该次回测实际使用的数量、阈值、成本和延迟。 |

复盘一笔交易时，依次核对：RFQ 是否 active 且未被占用；signal_bid 和 quote_price 是否算出正确 edge；保护限价是否正确；股票到达时 bid、BIDSIZE 和行情年龄是否合格；成交价是否严格高于保护价；RFQ 是否早于 expires_at 减安全缓冲确认；Token 成本是否为单价乘数量；redeem 时间是否正确；最终 PnL 是否能从成交金额重算。

## 12. 生产接入前必须补齐

正式上线前至少需要接入：

```text
正式 RFQ 的 quote_id、side、数量、总价和服务端 expiration
RFQ 执行、签名、广播、确认的真实状态
Alpaca 真实 order_id、成交回报和部分成交处理
借券检查、借券费和卖空限制
Fireblocks 自动审批与人工审批边界
redeem 失败、超时、重复提交和 journal 对账
真实 Gas、ITN 费用、稳定币兑换成本
异常时的状态未知、幂等和人工接管机制
```

在这些字段接入之前，本文和回测报告中的 completed 应理解为“按成功路径假设完成”，而不是生产交易的无条件成功。
