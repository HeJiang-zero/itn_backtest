# 回测结果文件说明

正常回测写入 `runs/backtests/backtest_<时间>_<参数哈希>/`；参数扫描写入 `runs/parameter_sweeps/`。开发验证不应写入这两处，而应使用临时目录或 `runs/validation/`。每个目录都有 `run_metadata.json`，其中 `run_kind` 明确标明产物类型。

| 文件 | 内容 | 何时查看 |
|---|---|---|
| `pnl_inventory_balance.png` | 上：已锁/已实现收益；中：股票、Token、Redeem pending、未对冲空头库存；下：现金、可用余额、保证金冻结和策略权益余额。 | 先看整体表现。 |
| `summary.json` | 交易次数、完成次数、拒绝原因、每日收益、总毛/净收益、初始/最终余额。 | 快速比较不同参数。 |
| `trade_details.parquet` | 一笔套利一行：RFQ、信号、股票腿、Token/Redeem 时间、保证金、全部延迟、毛/净 PnL 和关联订单 ID。 | 分析某笔套利是否赚钱。 |
| `order_details.parquet` | 每张模拟订单一行：股票卖空、RFQ 买入、Redeem、异常回补的提交/到场/成交/拒绝详情。 | 排查“每一步到底做了什么”。 |
| `opportunity_series.parquet` | 每个可评估时刻的最低价 RFQ、edge、报价池大小、触发的多笔交易及未交易原因。 | 画 edge 曲线、检查漏掉机会。 |
| `portfolio_state_series.parquet` | 仅在状态变化时记录库存、现金、Token 冻结、保证金冻结、总冻结资金、可用余额和已锁/已实现收益。 | 自定义画收益或库存曲线。 |
| `resolved_config.yaml` | 本次实际使用的完整参数快照。 | 复现结果。 |
| `input_data_manifest.json` | 输入文件名、哈希、行数、时间范围和标准化模式。 | 确认使用了哪批数据。 |
| `run_metadata.json` | run ID、`run_kind`、配置哈希、数据哈希、库存模型说明。 | 区分正式回测、参数扫描或验证。 |
| `debug_trace.parquet` | 仅当 `save_debug_trace: true` 时生成的局部状态轨迹。 | 定位程序问题时使用。 |

Parquet 文件适合用 Python、DuckDB、Excel/Power BI 插件或其他列式工具读取；字段含义可在 [回测设计](redeem_direction_backtest_design.md) 的“输出数据契约”章节查询。
